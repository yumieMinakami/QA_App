<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="jp.techacademy.yumie.minakami.qa_app.LoginActivity">

    <!--Edit Text for email address input -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="メールアドレス"
        android:id="@+id/emailText"
        android:layout_weight="1"/>

    <!-- EditText for password input -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:hint="パスワード(6文字以上)"
        android:id="@+id/passwordText"
        android:layout_weight="1"/>

    <!-- EditText for displayed name input -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="表示名"
        android:id="@+id/nameText"
        android:layout_weight="1"/>

    <!-- Button for login -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="ログイン"
        android:id="@+id/loginButton"
        android:background="@drawable/custom_button"
        android:textColor="@color/colorButtonText"
        android:layout_weight="1"/>

    <!-- Button for account creation -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="アカウント作成"
        android:id="@+id/createButton"
        android:background="@drawable/custom_button"
        android:textColor="@color/colorButtonText"
        android:layout_weight="1"/>

</LinearLayout>
